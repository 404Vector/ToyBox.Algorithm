#include <string>
#include <vector>
#include <iostream>
using namespace std;

namespace ToyBox::Algorithm{

    template <typename T>
    void CreatePermutation(std::vector<T> list, int n, int r);
    void Say(string s);
}
